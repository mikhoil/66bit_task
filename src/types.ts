export interface INew {
    title: string;
    content: string;
    id: number;
    createdAt: string;
    updatedAt: string;
}

export interface ITheme {
    id: number;
    name: string;
    mainColor: string;
    secondColor: string;
    title: string;
    textColor: string;
}
